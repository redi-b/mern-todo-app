const NavBar = () => {
  return (
    <div className="fixed flex items-center justify-center w-full p-4 bg-teal-700">
      <span className="text-4xl font-medium text-gray-50">Todo App</span>
    </div>
  );
};

export default NavBar;
